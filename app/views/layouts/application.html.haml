!!!
%html
  %head
    %title Mc12Muse
    =csrf_meta_tags
    =stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    =stylesheet_link_tag    'https://cdnjs.cloudflare.com/ajax/libs/normalize/5.0.0/normalize.css'
    =stylesheet_link_tag    'https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css'
    =stylesheet_link_tag    'https://fonts.googleapis.com/css?family=Roboto:100,300'
    =javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
  %body
    %header
      .wrapper.clearfix
        %logo= link_to 'MUSE', root_path
        %nav
          -if user_signed_in?
            =link_to current_user.name, edit_user_registration_path
            =link_to '+ Inspiration', new_post_path, class:'button'
            =link_to 'Sign Out', destroy_user_session_path, method: :delete, class:'button'
          -else
            =link_to 'Sign In', new_user_session_path
            =link_to 'Sign Up', new_user_registration_path, class:'button'

    -flash.each do |name, msg|
      =content_tag :p, msg, class:"#{name}"

    .wrapper
      =yield
